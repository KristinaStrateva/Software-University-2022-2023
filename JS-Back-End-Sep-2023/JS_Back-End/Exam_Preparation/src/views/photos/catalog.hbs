<main>
    <div class="container">
        <div class="col-9">
            {{#each photos}}
                {{>petCard}}
            {{else}}
            <!--If there are no photo yet!-->
            <article class="not-available-photo">
                <h1>No photo posts yet.</h1>
            </article>
            {{/each}}
        </div>

    </div>
</main>