function charecterSequence(input) {
    let text = input[0];
    let textLength = text.length;

    for (let i = 0; i < textLength; i++) {
        console.log(text[i]);
    }
}

charecterSequence(["softuni"]);